apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
  name: nfs-storage
  annotations:
    storageclass.kubernetes.io/is-defaiass: "true"
provisioner: fuseim.pri/ifs
parameters:
  archiveOnDelete: "true"

